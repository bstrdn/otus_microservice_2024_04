apiVersion: v1
kind: Service
metadata:
  name: {{ include "otusMicroservice.fullname" . }}
  labels:
    {{- include "otusMicroservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "otusMicroservice.selectorLabels" . | nindent 4 }}
