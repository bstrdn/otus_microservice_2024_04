apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "otusMicroservice.fullname" . }}-test-connection"
  labels:
    {{- include "otusMicroservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "otusMicroservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
